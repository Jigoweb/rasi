# Codebase Baseline

Data: 2026-06-23
Scope: inventario operativo prima di cleanup e refactor

## Stato Git

- Branch corrente: `feat/codebase-cleanup-roadmap`.
- Stato iniziale Task 0: working tree pulito prima della creazione dei documenti audit; primi file non tracciati attesi: `docs/codebase-audit/2026-06-23-baseline.md` e `docs/codebase-audit/branch-inventory.md`.
- Stato cumulativo dopo Task 0-7: la branch include modifiche di documentazione e tooling, tra cui `README.md`, `CONTRIBUTING.md`, `package.json`, `.env.example`, workflow GitHub Actions, documenti in `docs/` e `server/.env.example`.
- Scope modifiche sorgenti: nessun file applicativo modificato in Task 7; `next.config.ts` lasciato invariato perché la baseline lint non è green.
- Worktree: tre worktree locali rilevate: `/Users/matteo/rasi` su `main`, `/Users/matteo/rasi/.worktrees/codebase-cleanup-roadmap` su `feat/codebase-cleanup-roadmap`, `/Users/matteo/rasi/.worktrees/matching-reliability` su `feat/matching-reliability-optimization`.
- Branch locali: vedere `docs/codebase-audit/branch-inventory.md`.

## Verifiche Disponibili

| Comando | Scopo | Stato baseline | Note |
|---|---|---|---|
| `npm run lint` | ESLint frontend | FAIL | 491 errori: 394 `no-explicit-any`, 63 `no-unused-vars`, 32 `react/no-unescaped-entities`, 1 `ban-ts-comment`, 1 `no-empty-object-type` |
| `npm run typecheck` | TypeScript frontend | PASS | Nessun errore di tipo |
| `npm test` | Test Jest frontend | PASS | 15 suite, 199 test, 0 falliti |
| `npm --prefix server run typecheck` | Typecheck worker | PASS | Nessun errore di tipo nel worker |
| `npm run build` | Build Next.js | PASS | Build completata, ESLint ignorato durante build da `next.config.ts` |
| `npm run verify` | Pipeline completa | FAIL | Fallisce allo step lint (primo step della catena) |

### Nota Task 5 Tooling

Task 5 ha introdotto `npm run verify`, che combina lint, typecheck root, test, build e typecheck worker. In Task 7 `npm run typecheck` passa nella worktree e la build baseline è PASS; `npm run build` completa con ESLint ignorato durante build da `next.config.ts`. La baseline lint resta non green con il comando effettivamente usato in Task 7: `npm run lint`.

### Nota Task 6 CI

Task 6 ha aggiunto il workflow GitHub Actions `Verify` per pull request e push su `main`/`develop`. La CI non va ancora considerata obbligatoriamente green: Task 7 resta proprietaria della triage baseline lint/build e dell'eventuale classificazione dei fallimenti.

## Lint Gate Decision

`next.config.ts` still ignores ESLint during build because baseline lint is not green. Remove `eslint.ignoreDuringBuilds` after lint errors are triaged.

Comando lint eseguito: `npm run lint`.

Categorie hotspot lint da triagare nella prossima task:
- **394** `@typescript-eslint/no-explicit-any` — sostituire `any` con tipi espliciti
- **63** `@typescript-eslint/no-unused-vars` — rimuovere import e variabili inutilizzate
- **32** `react/no-unescaped-entities` — escaped apostrofi/virgolette nei JSX (`&apos;`, `&quot;`)
- **1** `@typescript-eslint/ban-ts-comment` — sostituire `@ts-ignore` con `@ts-expect-error`
- **1** `@typescript-eslint/no-empty-object-type` — rimuovere interfaccia vuota

Hotspot per file: estrazione file-level da completare come follow-up; in Task 7 sono state catturate le categorie aggregate senza rerun dettagliato completo.

## Rischi Iniziali

- Branch locali multipli da classificare prima del cleanup.
- Worktree attiva da non modificare senza conferma.
- Documentazione setup/env disallineata.
- Pagine dashboard grandi da non refactorare senza test/checklist.
- Workflow individuazioni duplicato da chiarire prima di rimuovere codice legacy.

## Decisioni Aperte

- Branch base del lavoro.
- Percorso UI canonico.
- Percorso individuazioni canonico.
- Formato guida AI: `AGENTS.md`, `.cursor/rules`, o entrambi.
